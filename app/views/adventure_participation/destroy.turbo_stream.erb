<%= turbo_stream.update dom_id(@character, :button) do %>
	<%= turbo_frame_tag dom_id(@character, :button), class: "col text-center" do %>
		<%= button_to "Add", create_adventure_participation_path(id: @adventure.id), params: { adventure_participation:{ character_id: @character.id }}, class: "btn btn-primary btn-sm" %>
	<% end %>
<% end %>

<%= turbo_stream.remove dom_id(@character, :content) %>
<%= turbo_stream.remove dom_id(@character, "offcanvas") %>